# Android test builds

Every push to `main` triggers the `Android APK` GitHub Actions workflow.

When a build succeeds, open **Actions → Android APK → latest run**, then download the `LifeAdmin-Android-debug` artifact. Extract the ZIP and install `LifeAdmin-latest-debug.apk` on an Android device.

Debug APKs are for internal testing and are not Play Store release artifacts.
